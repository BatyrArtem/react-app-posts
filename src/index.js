import React from 'react';
import ReactDOM from 'react-dom';  // импортируенм ядро реакта
import App from './App';

ReactDOM.render(   // html разметка внутри js файла - формат jsx
    <App/>,
    document.getElementById('root')
);

